# Dressage enclosure backgrounds moved (Assets 2.0 Phase 2 — lot Background)

Runtime source-of-truth: `assets/Background/{biomeId}/dressage-wide.png` and `dressage-portrait.png`

Served at `/assets/minigames/dressage/enclosures/{biomeId}.png` via unified Vite plugin (`vite.repo-assets.ts` → `repoAssetsPlugin`).

Do not re-add files here — update `assets/Background/` instead.
