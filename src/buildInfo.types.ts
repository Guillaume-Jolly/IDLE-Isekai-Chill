export type AppBuildInfo = {
  semver: string
  commitCount: number
  commitHash: string
  dirty: boolean
  revision: number
  subRevision: number
  build: string
  versionLabel: string
  builtAt: string
}
